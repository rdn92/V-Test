export class Todo {
    // id: number;
    // title: string = '';
    // done: boolean = false;
    // priority: string = "";
    // position: number;

    constructor(public id: number, public title: string, public done: boolean, public priority: string,
                public position: number) { }
}